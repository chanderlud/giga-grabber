name: Build Release

on:
  push:
    tags: "v*"

env:
  CARGO_TERM_COLOR: always

# contents: create the GitHub Release (release job)
# id-token: mint OIDC token for Sigstore signing
# attestations: persist signed attestations
# artifact-metadata: create artifact storage record
permissions:
  contents: write
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: [stable]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Install Dependencies
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev

      - name: Compute VERSION (strip leading v)
        id: vars
        shell: bash
        run: |
          ref="${GITHUB_REF_NAME}"
          if [[ "$ref" == v* ]]; then
            ver="${ref#v}"
          else
            ver="$ref"
          fi
          echo "version=$ver" >> "$GITHUB_OUTPUT"
          echo "Using VERSION=$ver"

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Build (debug)
        run: cargo build --verbose

      - name: Build (release for tagged builds)
        if: startsWith(github.ref, 'refs/tags/')
        run: cargo build --release

      # -------------------- Windows --------------------
      - name: Package Windows binary
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $version = "${{ steps.vars.outputs.version }}"
          $out = "giga-grabber-$version-windows-standalone-x86_64.exe"
          Write-Host "Output: $out"
          Move-Item "target/release/giga_grabber.exe" "target/release/$out"

      - name: Attest Windows binary
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'windows-latest'
        uses: actions/attest@v4
        with:
          subject-path: target/release/giga-grabber-${{ steps.vars.outputs.version }}-windows-standalone-x86_64.exe

      - name: Upload Windows artifact
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v6
        with:
          name: giga-grabber-${{ steps.vars.outputs.version }}-windows-standalone-x86_64
          path: target/release/giga-grabber-${{ steps.vars.outputs.version }}-windows-standalone-x86_64.exe

      # -------------------- Linux --------------------
      - name: Package Linux binary
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          OUT="giga-grabber-${{ steps.vars.outputs.version }}-linux-standalone-x86_64"
          echo "Output: ${OUT}"
          mv target/release/giga_grabber "target/release/${OUT}"

      - name: Attest Linux binary
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'ubuntu-latest'
        uses: actions/attest@v4
        with:
          subject-path: target/release/giga-grabber-${{ steps.vars.outputs.version }}-linux-standalone-x86_64

      - name: Upload Linux artifact
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v6
        with:
          name: giga-grabber-${{ steps.vars.outputs.version }}-linux-standalone-x86_64
          path: target/release/giga-grabber-${{ steps.vars.outputs.version }}-linux-standalone-x86_64

      # -------------------- macOS (aarch64) --------------------
      - name: Package macOS binary
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'macos-latest'
        shell: bash
        run: |
          OUT="giga-grabber-${{ steps.vars.outputs.version }}-macos-standalone-aarch64"
          echo "Output: ${OUT}"
          mv target/release/giga_grabber "target/release/${OUT}"

      - name: Attest macOS binary
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'macos-latest'
        uses: actions/attest@v4
        with:
          subject-path: target/release/giga-grabber-${{ steps.vars.outputs.version }}-macos-standalone-aarch64

      - name: Upload macOS artifact
        if: startsWith(github.ref, 'refs/tags/') && matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v6
        with:
          name: giga-grabber-${{ steps.vars.outputs.version }}-macos-standalone-aarch64
          path: target/release/giga-grabber-${{ steps.vars.outputs.version }}-macos-standalone-aarch64

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v6
        with:
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          files: dist/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
